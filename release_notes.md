## Changelog

### Added
- **裝置畫面投影（連拍）**：在「裝置選擇」區塊新增「裝置畫面投影」按鈕，開啟可自由拖曳/縮放的浮動視窗，透過 USB 連續擷取 iOS 裝置畫面（約 1–2 fps，可調整更新頻率），方便對齊遊戲內地圖與模擬位置，不必一直在手機與電腦間切換。使用獨立的 DVT 連線，不影響定位模擬；最小化時自動暫停以節省資源。
- **應用程式內自動更新**：偵測到新版時可直接在程式內一鍵下載並安裝，下載過程顯示進度，完成後自動重新啟動，不再需要手動到下載頁覆蓋檔案。非打包執行（從原始碼執行）時仍維持開啟下載頁的行為。
- **行走時間與剩餘時間預估**：路線模式現在會顯示距離與預估耗時；開始行走後，畫面會同步顯示剩餘距離與剩餘時間，方便使用者快速掌握還要走多久。
- **移動設定面板**：新增「移動設定」入口，將路線移動相關的擬真選項集中管理，不需要再分散在主畫面上找不同控制項。
- **路線移動擬真選項**：新增隨機短暫停留與位移噪音，可讓移動過程看起來更像真實走路或騎乘中的自然停頓與微幅偏移。

### Changed
- **移動控制介面整理**：主畫面將速度噪音改為收納進「移動設定」面板，並加入步行、騎車、開車的快速速度按鈕，常用操作更直覺。
- **移動設定會自動記住**：速度噪音、隨機停留與位移噪音等選項，現在會在重新開啟 App 後保留，不需要每次重新設定。
- **側邊欄排版優化**：整理左側控制區塊的按鈕寬度、上下間距與區塊節奏，讓主要操作更一致、更容易掃視。
- **清除與傳送按鈕一致化**：手動座標區塊中的「瞬間移動」與「清除模擬定位」按鈕寬度與排列方式已統一，整體視覺更穩定。
- **設定提示更好懂**：移動設定中的每個主要選項旁邊都加入問號提示，改用更接近使用者語言的方式說明功能效果。

### Fixed
- **更新時的檔案鎖死問題**：修正關閉程式後 tunneld 背景子行程殘留、鎖住 `iFakeGPS.exe` 導致無法覆蓋更新（過去需重開機）的問題。關閉時改為終結整棵 tunneld 行程樹，並清除佔用 tunneld 連接埠的殘留行程。
- **行走中時間顯示不夠即時**：修正路線移動時仍只顯示全程預估的問題，現在會依照目前進度與暫停狀態持續更新剩餘時間。
- **移動軌跡過於死板**：改善路線模擬時的移動表現，避免看起來像完美直線貼著路徑前進。
- **移除無線模式入口**：暫時移除主畫面上的「開啟無線模式」按鈕，降低不穩定功能對一般使用流程的干擾。
